# Public site URL (no trailing slash). Used at build time.
# Production: your deployed Next.js URL (e.g. Vercel).
# Local dev on same Wi‑Fi: http://YOUR_PC_LAN_IP:3000
EXPO_PUBLIC_SITE_URL=https://your-site.example.com
