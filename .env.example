# NextAuth — required in production (generate: openssl rand -base64 32)
NEXTAUTH_SECRET=
# Site URL (no trailing slash) — use your Vercel URL in production
NEXTAUTH_URL=http://localhost:3000

# Member portal — single account for demo; replace with DB or IdP later
MEMBER_EMAIL=member@example.com
MEMBER_PASSWORD=
