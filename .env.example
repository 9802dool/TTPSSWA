# Optional: Resend — preferred for hotel booking notifications (https://resend.com)
# If unset, bookings use FormSubmit as a fallback (no API key required).
RESEND_API_KEY=

# Booking emails go here (default: simeondoolarsingh@hotmail.com)
# HOTEL_BOOKING_NOTIFY_EMAIL=

# Optional: verified sender when using Resend (default: onboarding@resend.dev)
# BOOKING_FROM_EMAIL=TTPSSWA Bookings <bookings@yourdomain.com>
